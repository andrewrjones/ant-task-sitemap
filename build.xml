<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="ant-task-sitemap" basedir="." default="test">

    <property name="src.dir" value="src"/>
    <property name="test.dir" value="test"/>
    <property name="lib.dir"   value="lib"/>
    <property name="testlib.dir"   value="testlib"/>
    <property name="classes.dir" value="classes"/>
    <property name="report.dir"   value="report"/>
    <property name="junit.out.dir.xml"  value="${report.dir}/junit/xml"/>
    <property name="junit.out.dir.html" value="${report.dir}/junit/html"/>

    <path id="classpath.run">
        <path path="${java.class.path}"/>
        <path location="${ant.project.name}.jar"/>
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>

    <path id="classpath.test">
        <path refid="classpath.run"/>
        <path location="${ant.test.lib}"/>
        <fileset dir="${testlib.dir}" includes="**/*.jar"/>
    </path>

<!-- clean directories -->
    <target name="clean" description="Delete all generated files">
        <delete failonerror="false" includeEmptyDirs="true">
            <fileset dir="." includes="${ant.project.name}.jar"/>
            <fileset dir="${classes.dir}"/>
            <fileset dir="${report.dir}"/>
        </delete>
        <mkdir dir="${classes.dir}"/>
    </target>

<!-- compile tasks -->
    <target name="compile" description="Compiles the source">
        <javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath.run"/>
    </target>
    
    <target name="compile-test" description="Compiles the tests">
        <javac srcdir="${test.dir}" destdir="${classes.dir}" classpathref="classpath.test"/>
    </target>

<!-- create jar -->
    <target name="jar" description="JARs the Task" depends="compile">
        <jar destfile="${ant.project.name}.jar" basedir="${classes.dir}"/>
    </target>
    
<!-- 'use' tasks for our new task -->
    <target name="use.without" description="Use without any files" depends="jar">
        <taskdef name="sitemap" classname="uk.co.arjones.ant.task.Sitemap" classpath="${ant.project.name}.jar"/>
        <sitemap/>
    </target>
    
<!-- test tasks -->
    <target name="junit" description="Runs the unit tests" depends="compile-test, jar">
        <delete dir="${junit.out.dir.xml}"/>
        <mkdir  dir="${junit.out.dir.xml}"/>
        <junit printsummary="yes" haltonfailure="yes">
            <classpath refid="classpath.test"/>
            <formatter type="xml"/>
            <batchtest fork="yes" todir="${junit.out.dir.xml}">
                <fileset dir="${test.dir}" includes="**/*Test.java"/>
            </batchtest>
        </junit>
    </target>

    <target name="junitreport" description="Create a report for the rest result">
        <mkdir dir="${junit.out.dir.html}"/>
        <junitreport todir="${junit.out.dir.html}">
            <fileset dir="${junit.out.dir.xml}">
                <include name="*.xml"/>
            </fileset>
            <report format="frames" todir="${junit.out.dir.html}"/>
        </junitreport>
    </target>

    <target name="test"
            depends="junit,junitreport"
            description="Runs unit tests and creates a report"
    />

</project>